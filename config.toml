# The URL the site will be built for
base_url = "https://nboisvert.com"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
name = "Nicolas Boisvert"
fun = ["Rust", "Haskell"]
knowledge = ["Elixir", "Elm", "Javascript", "Typescript", "C#", "Java", "PHP"]
job_name = "theScore"
job_link = "https://thescore.com"
nav = [
  { name = "Home", link = "/" },
  { name = "Blog", link = "/blog/" },
  { name = "Talks", link = "/talks/" },
  { name = "Sandbox", link = "/sandbox/" },
]
handles = [
  { name = "GitHub", link = "https://github.com/nicklayb" },
]
email = "nicolas@nboisvert.com"
